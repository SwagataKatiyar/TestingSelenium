import java.util.LinkedList;
import java.util.Queue;
import java.util.Scanner;

public class MyThread1 extends Thread {
	Scanner sc = new Scanner(System.in);
	Queue<Shapes> q = new LinkedList<Shapes>();
	public void getShapes(Queue<String> q) {
		   
	}

	public void run() {

		int no;
		do {
			System.out.println("Choose from the list:");
			System.out.println("1.Calculate for Square");
			System.out.println("2.Calculate for Rectangle");
			no = sc.nextInt();
			switch (no) {
			case 1:
				System.out.println("Enter the number");
				int n1= sc.nextInt();
				Shapes s1= new Rectangle(n1,n2);
				q.add();
				break;
			case 2:
				q.add("Rectangle");
			}
		} while (no == 1 || no == 2);
		System.out.println("Choose correctly from the list.");

	}
}